import { Code, Drawer, ScrollArea, Stack, Table, Text } from '@mantine/core';
export default function ScreenMetaDrawer({ spec, opened, onClose }) { return <Drawer opened={opened} onClose={onClose} title={`${spec.id} · Screen specification`} position="right" size="lg"><ScrollArea h="calc(100vh - 90px)"><Stack><Text size="sm"><b>Route:</b> <Code>{spec.route}</Code></Text><Text size="sm"><b>BRD:</b> {spec.brd || '—'}</Text><Text size="sm"><b>Users:</b> {spec.users || '—'}</Text>{spec.contract && spec.contract.length > 0 && <Table withTableBorder withColumnBorders><Table.Tbody>{spec.contract.map((r)=><Table.Tr key={r[0]}><Table.Td w={160}><b>{r[1]}</b></Table.Td><Table.Td>{r[2]}</Table.Td></Table.Tr>)}</Table.Tbody></Table>}</Stack></ScrollArea></Drawer>; }
